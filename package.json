{
  "name": "boli-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "backend",
    "guest-app",
    "merchant-dashboard",
    "admin-portal"
  ],
  "scripts": {
    "dev": "concurrently -n backend,guest,merchant,admin -c blue,green,magenta,yellow \"npm run dev --workspace=backend\" \"npm run dev --workspace=guest-app\" \"npm run dev --workspace=merchant-dashboard\" \"npm run dev --workspace=admin-portal\"",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=backend && npm run build --workspace=guest-app && npm run build --workspace=merchant-dashboard && npm run build --workspace=admin-portal",
    "prisma:migrate": "npm run prisma:migrate --workspace=backend",
    "prisma:seed": "npm run prisma:seed --workspace=backend",
    "campaigns:run": "npm run campaigns:run --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
